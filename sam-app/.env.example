VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL del webhook (n8n / Zapier) que recibe los textos dictados desde la app.
# Se llama fire-and-forget desde DictateButton; si está vacío, el botón sigue
# pegando el texto en el textarea pero no envía nada.
VITE_DICTATE_WEBHOOK_URL=
